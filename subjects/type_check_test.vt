// Test 1: Function call arity checking
fn add(a: i32, b: i32): i32 {
    return a + b;
}

fn test_arity(): i32 {
    let x = add(1, 2);
    let y = add(1);
    let z = add(1, 2, 3);
    return x;
}

// Test 2: Function return type checking (already exists in type checker)
fn returns_int(): i32 {
    return 42;
}

fn returns_wrong(): i32 {
    return true;
}

// Test 3: Cast type conversion checking
fn test_casts(): i32 {
    let a: i32 = 42;
    let b: i64 = a as i64;
    let c: f32 = a as f32;
    let d: bool = a as bool;
    return 0;
}
